receive text messages when a sender emails you.

( Gmail API , Firebase serverless , React Native ) .


running the native project.
  npm install
  npm install --only=dev

  expo install react-native-safe-area-context
